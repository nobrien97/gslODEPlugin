# This starter workflow is for a CMake project running on multiple platforms. There is a different starter workflow if you just want a single platform.
# See: https://github.com/actions/starter-workflows/blob/main/ci/cmake-single-platform.yml
name: CMake on multiple platforms

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      # Set fail-fast to false to ensure that feedback is delivered for all matrix combinations.
      fail-fast: false

      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        build_type: [Release]
        host_c_compiler: [gcc, clang]
        plugin_c_compiler: [gcc, clang]
        include:
          - os: windows-latest
            generator: "MinGW Makefiles"
          - host_c_compiler: gcc
            host_cpp_compiler: g++
          - host_c_compiler: clang
            host_cpp_compiler: clang++
          - plugin_c_compiler: gcc
            plugin_cpp_compiler: g++
          - plugin_c_compiler: clang
            plugin_cpp_compiler: clang++
        exclude:
        - os: windows-latest
          host_c_compiler: clang

    steps:
    - uses: actions/checkout@v4

    - name: Set reusable strings
      # Turn repeated input strings (such as the build output directory) into step outputs. These step outputs can be used throughout the workflow file.
      id: strings
      shell: bash
      run: |
        echo "build-output-dir=${{ github.workspace }}/build" >> "$GITHUB_OUTPUT"

    - name: Install MinGW
      if: matrix.os == 'windows-latest'
      run: |
        choco install mingw --no-progress
        echo "C:\ProgramData\chocolatey\lib\mingw\tools\install\mingw64\bin" >> $GITHUB_PATH

    - name: Configure CMake plugins
      # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
      # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
      run: >
        cmake -B ${{ steps.strings.outputs.build-output-dir }}
        -S ${{ github.workspace }}
        -DCMAKE_CXX_COMPILER=${{ matrix.plugin_cpp_compiler }}
        -DCMAKE_C_COMPILER=${{ matrix.plugin_c_compiler }}
        -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}
        -DBUILD_ONLY_LIBS=ON
        -DBUILD_ONLY_MAIN=OFF
        ${{ matrix.generator && format('-G "{0}"', matrix.generator) || '' }}
    - name: Build plugins
      # Build your program with the given configuration. Note that --config is needed because the default Windows generator is a multi-config generator (Visual Studio generator).
      run: |
        cmake --build ${{ steps.strings.outputs.build-output-dir }} --config ${{ matrix.build_type }}
    - name: Configure CMake host
      # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
      # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
      run: >
        cmake -B ${{ steps.strings.outputs.build-output-dir }}
        -S ${{ github.workspace }}
        -DCMAKE_CXX_COMPILER=${{ matrix.host_cpp_compiler }}
        -DCMAKE_C_COMPILER=${{ matrix.host_c_compiler }}
        -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}
        -DBUILD_ONLY_LIBS=OFF
        -DBUILD_ONLY_MAIN=ON
        ${{ matrix.generator && format('-G "{0}"', matrix.generator) || '' }}
    - name: Build host
      # Build your program with the given configuration. Note that --config is needed because the default Windows generator is a multi-config generator (Visual Studio generator).
      run: |
        cmake --build ${{ steps.strings.outputs.build-output-dir }} --config ${{ matrix.build_type }}
    - name: Test
      uses: threeal/ctest-action@v1.1.0
      with:
        test-dir: ${{ steps.strings.outputs.build-output-dir }}
        build-config: ${{ matrix.build_type }}
        args: -L gh --extra-verbose
      # Execute tests defined by the CMake configuration. Note that --build-config is needed because the default Windows generator is a multi-config generator (Visual Studio generator).
      # See https://cmake.org/cmake/help/latest/manual/ctest.1.html for more detail
      # run: ctest --build-config ${{ matrix.build_type }} --output-on-failure -L gh
